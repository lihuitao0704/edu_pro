# 智能财富管家系统 — 项目评审报告

**评审方**：甲方（尖酸刻薄版）
**评审日期**：2026-07-25
**项目规模**：后端 ~29,000 行 Python / 前端 ~5,500 行 Vue3+TS / 测试 ~5,500 行


## 一、总体印象

一个"能跑"但"不够好"的系统。功能骨架是完整的，细节上像被五个不同的人各写了一段然后拼在一起。有一种强烈的"赶工交付"气味——该有的都有，但该好的不好。


## 二、优点（先给颗糖，再扇巴掌）

### 1. 多 Agent 架构选型正确
Router -> Advisor/Customer/Operator/Risk 的分发模式，配合 LangChain 的 tool-calling，LLM 自主决定调用哪个工具。这比写死 if/elif 分支高明。`smart_recommend` 一键推荐工具（画像+风控+推荐+配置一次调用完成）封装务实。`OperatorAgent` 的 `_TOOL_REGISTRY` 注册模式支持可扩展的工具链。

### 2. 双数据库设计有远见
MySQL 管交易和画像，Neo4j 管图谱关系。`graph_sync` 的事件驱动增量同步 + 定时补偿是正确架构思路。

### 3. 风控引擎有深度
四维度打分 + 熔断规则（block/restrict/warning 三级）+ 双轨校准 + 特殊场景处理。不是玩具系统。

### 4. 统一对话入口设计干净
`POST /api/chat` 一个口子进，RouterAgent 分发，`UnifiedChatResponse` 统一包装。前端不需要知道后面有几个 Agent。`ChatOrchestrator` 作为横切管道，统一施加安全过滤、追踪、实体跟踪。

### 5. E2E 测试框架实用
能够直接验证 MySQL + Neo4j 数据落库，不是只测 HTTP 状态码的表面测试。

### 6. 前端设计系统基础扎实
CSS 自定义属性（`--finance-bg` 等）、深色主题、`EmptyState`/`LoadingPanel`/`ErrorAlert` 通用组件。响应式断点 1180px / 760px 一致。

### 7. 业务校验体系全面
`operator_agent.py` 实现了 P0-P5 全量校验：适当性匹配、交易时间、日累计限额、风险标记、AML 检查、客户状态、产品状态——覆盖了申购/赎回/转账/工单全流程。


## 三、问题清单（开始扇巴掌）

### 🔴 P0 — 安全红线

**1. `AUTH_MOCK_MODE=true` + `.env` 在 git 里**
`app/middleware/auth.py:86-92`：Mock 模式开着时，任何请求以管理员身份执行。`.env` 里的 API key、数据库密码全部明文且被 git tracked。一旦代码泄露，全套生产凭据拱手送人。

**2. 全局异常处理器泄露堆栈**
`app/middleware/exception_handler.py:39-40`：`"服务器内部错误: {str(exc)}"` 把堆栈、SQL schema、文件路径直接返回给 API 调用方。金融系统的基本安全红线。

### 🔴 P1 — 架构/数据一致性

**3. Neo4j 图谱是一锅粥**
12 种节点标签、11 种关系类型。`neo4j_import.py`（全量导入）+ `graph_sync.py`（增量同步）+ `neo4j_sync.py`（业务同步）三个脚本各自为政。`Transaction` 节点和 `MADE`/`ON_PRODUCT`/`HANDLED_BY` 关系链建了但推荐引擎完全不用。`RELATED_TO` 关系建了又删、删了又建。Neo4j 成了垃圾桶，什么数据都往里倒，能用的没几条。

**4. 推荐引擎是"假图推荐"**
号称用 Neo4j 做推荐，实际推荐引擎用的 `MOCK_PRODUCTS` 是代码里写死的 9 个产品，数据库 `fin_product` 表 37 个产品完全不查。三路 Cypher 多跳查询依赖的 `BELONGS_TO` 行业关系大多缺失，查出来结果稀稀拉拉。

**5. `operator_agent.py` 1710 行单体巨石**
LLM 客户端、业务常量、RBAC 权限、16 个校验函数、17 个工具处理器、Redis 确认状态管理、审计日志——全塞在一个文件里。改一个校验函数要在这个文件里翻半天。

**6. 三处重复的风控上下文查询**
`RouterAgent._risk_precheck()`、`CustomerServiceAgent._query_risk_context()`、`ChatOrchestrator._check_risk_context()` ——三个地方独立查询 Redis + MySQL 获取客户风险标记，实现略有不同。敏感关键词列表也在两个文件里分别定义。

**7. 数据库无参照完整性**
所有 ForeignKey 没有 `CASCADE`、没有 `ON DELETE`。`fin_holdings.product_id` 引用 `fin_product.id`，产品删了持仓变悬空引用。完整性全靠应用层祈祷。

**8. `except Exception` 满天飞 — 173 处**
风控查询失败？吞掉。图谱查询失败？吞掉。记忆召回失败？吞掉。Redis 写入失败？吞掉。全部在"不影响主流程"的注释下静默降级。生产出问题没人知道什么坏了。

### 🟡 P2 — 代码质量/可维护性

**9. 前端单文件巨兽**
`AdvisorWorkspaceView.vue` 575 行，`RiskManagementView.vue` 567 行。一个组件扛起全部逻辑：数据获取、状态管理、sessionStorage 缓存、视图切换——没有任何 composable 或子组件拆分。

**10. 前端视图层零测试**
416 行测试只覆盖 store、router、API 工具函数。575 行的 `AdvisorWorkspaceView` 没有任何测试。之前嵌套 allocation 结构 bug（饼图不显示）——如果有测试就不会存在。

**11. `RiskService.__new__()` 反模式**
`app/api/risk.py:49` 用 `RiskService.__new__(RiskService)` 创建未初始化对象来调 `get_questionnaire()`。这个方法应该是静态方法。

**12. ProfileService 7 次顺序查询无并行**
`_collect_customer_data()` 做 7 次顺序数据库查询，`asyncio.gather()` 并行化可获得 2-3x 速度提升。

**13. Prompt 模板每次请求从磁盘读取**
`CustomerServiceAgent` 的 prompt 模板每次请求都 `Path.read_text()` 同步读取。启动时加载一次到内存即可。

**14. NL2SQL 端点用 `code=1003` 表示预期业务错误**
前端需分别处理 `code=1003`（拒绝查询）和 `code=500`（服务器错误），但 `ApiResponse` 只定义了 `code=200`。

**15. `operator_agent.py` 用 `LIKE '%%✅%%'` 找出历史操作**
用 emoji 匹配来识别历史操作记录——如果哪天前端换了图标，这个逻辑就静默失效了。

### 🟢 P3 — 体验/规范性

**16. TypeScript `any` 类型滥用 — 35 处**
`profile = ref<Record<string, any> | null>(null)`。画像核心数据结构逃掉类型检查。

**17. 前端指标卡片不统一**
`ProfileView` 和 `AdvisorWorkspaceView` 的五指标卡片 HTML 几乎一样但没有抽组件。改样式要改两个地方。

**18. 组件空状态不统一**
`AllocationPieChart`、`HoldingPanel`、`RecommendationGrid` 各写各的 `state-panel` 和 emoji。`EmptyState` 通用组件明明存在却被忽略。

**19. CSS 变量和硬编码混用**
`ChatWindow.vue`、`MessageCard` 等组件在 scoped style 里直接用 `#334155`、`#0f172a`，而不是 `var(--finance-line)`。

**20. LangChain Agent 无缓存/池化**
每次 `AdvisorAgent` 请求创建新 LangChain agent 实例。`recursion_limit=6` 和 `timeout=180` 硬编码而非配置。

**21. `dimension_calculator.py` 有 `if __name__ == "__main__"` 的手动测试**
写在源文件里而不是 pytest——只有手动 `python dimension_calculator.py` 才会跑。

**22. 前端无全局错误边界**
唯一的全局错误处理是 401 自动登出。网络错误、500 错误全靠每个视图各自处理。`ChatWindow` 的 `hydrateHistory()` 用空 catch 吞掉所有错误。


## 四、亮点建议

### 1. 实时图谱可视化
Neo4j Bloom 或 D3.js 做客户关联网络视图。`RELATED_TO` 关系已算好，画出来就是现成的演示亮点——投顾看到客户A选了产品X，图谱上看到同样持有X的客户B还买了什么。

### 2. 推荐理由可解释性
现在推荐理由是"该产品为R3级混合基金，预期年化7%，与您的平衡型风险偏好匹配"。加上图数据后可以改成"您同风险等级的12位客户中有8位持有此产品，3位与您持仓重合的客户也购买了它"——比"匹配度85%"有说服力十倍。

### 3. 完整 Demo 链路演示
你们已有注册->风评->购买的 E2E 测试。把这做成可视化——从客户注册、到风险测评、到产品推荐、到一键购买、到 MySQL 落库、到 Neo4j 同步、到图谱可视化，一气呵成。

### 4. 推荐效果 A/B 对比
界面上加开关——"规则引擎 vs 图推荐"，让投顾直观看到两种推荐方式的差异。答辩时最有说服力的 slide。

### 5. 智能预警实时推送
`FinRiskAlert` 表已有数据。加 WebSocket 通道，高风险预警实时推送到投顾工作台和风控管理页面。


## 五、技术债务优先级

| 优先级 | 问题 | 工作量 |
|--------|------|--------|
| P0 | 关 `AUTH_MOCK_MODE`、`.env` 出 git | 5 分钟 |
| P0 | 异常处理器去堆栈 | 2 分钟 |
| P1 | Neo4j Schema 统一重建 | 1 天 |
| P1 | 推荐引擎从 DB 读产品 | 2 小时 |
| P1 | 前端核心视图加测试 | 2 天 |
| P1 | 风控上下文查询去重（三合一） | 2 小时 |
| P1 | DB 加参照完整性 | 1 小时 |
| P2 | `operator_agent.py` 拆分 | 1 天 |
| P2 | 抽 composable + 组件 | 1 天 |
| P2 | `except Exception` 分类处理 | 1 天 |
| P2 | ProfileService 查询并行化 | 1 小时 |
| P3 | TypeScript `any` 收敛 | 1 天 |
| P3 | CSS 变量统一 | 2 小时 |
| P3 | Prompt 模板启动时加载 | 30 分钟 |


## 六、总分

| 维度 | 评分 | 说明 |
|------|------|------|
| 架构设计 | 4/5 | 多 Agent + 双数据库 + 事件驱动，思路正确 |
| 代码质量 | 2/5 | 单体文件、any 泛滥、异常吞没、反模式 |
| 业务完整性 | 3/5 | 注册->风评->推荐->购买链路完整 |
| 测试覆盖 | 2/5 | 逻辑层有测试，视图层裸奔 |
| 数据一致性 | 2/5 | MySQL 和 Neo4j 两张皮 |
| 可维护性 | 2/5 | 改一个功能翻三个文件 |
| 安全性 | 1/5 | Mock 模式开着，密钥在 git，堆栈外泄 |
| 创新性 | 3/5 | 图推荐有概念但落地不够 |

**综合评分：2.5 / 5**

能演示，不能上线。骨架是研究生毕设水平，细节是外包作坊水平。花一周把 P0/P1 修了，能到 3.5 分。
