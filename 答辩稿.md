# 数据分析与总体框架优化答辩逐字稿

> 建议时长：约 6–8 分钟。方括号中的内容为页面或操作提示，答辩时不必朗读。

## 开场

各位评委老师，大家好。我今天汇报的内容主要分为两部分：第一部分是我负责的数据分析 Agent；第二部分是我参与优化的总体框架。

我想解决的核心问题，不是简单地让用户“用自然语言写 SQL”，而是让金融业务数据做到三件事：第一，员工能够方便地问；第二，系统能够安全地查；第三，查出来的结果能够被投顾和风控安全地使用。

[打开 `数据分析.html` 首屏]

我的结论是：我把数据分析从一个独立查询功能，做成了“经营数据、可信分析、受控事件、业务行动”的闭环。

## 第一部分：我负责的数据分析 Agent

[切到“数据分析 Agent 主流程”]

先介绍整体流程。内部员工提出一个自然语言问题，例如“统计近三十天各等级风险预警数量”。请求会先经过 Router Agent，识别为 data_analysis 意图，并在统一路由层做角色校验。

随后进入我负责的 NL2SQL 服务。服务不会把整个数据库结构不加区分地交给大模型，而是先根据“持仓、预警、AUM、风评”等业务关键词选出相关表，再组合 Schema、表关系和 few-shot 示例，引导模型生成更贴近业务语义的 SQL。

但模型生成的 SQL 只是候选语句，不是可以直接执行的语句。这是我的设计重点。

[切到“五道安全闸门”]

我为数据分析链路设计了五道安全闸门。

第一道是意图预检。问候、闲聊、过短输入和非业务问题不会进入 SQL 生成流程，系统会引导用户提出业务数据问题。

第二道是最小 Schema 原则。按问题选择相关表，并把表关系补充给模型，既降低 SQL 幻觉，也减少不相关字段进入上下文。

第三道是只读校验。系统会先剥离 SQL 注释，随后要求语句必须以 SELECT 开头，禁止所有写操作，比如 DELETE、UPDATE、INSERT 和 DROP。

第四道是危险语法拦截。即使是 SELECT，也会拒绝 UNION、INTO、LOAD_FILE、EXEC、EXECUTE 和多语句，避免通过变形语法绕过限制。

第五道是敏感数据和资源边界。密码、令牌、身份证等敏感列既不允许出现在 SQL 中，执行结果也会再次过滤并做脱敏；同时单次结果最多返回一百行，缓存按用户 ID 和问题 hash 隔离，默认保存十分钟。

因此，我这里强调一句：大模型只负责理解问题和生成候选 SQL，真正的执行权仍然在规则校验和数据库权限边界手里。这是金融数据分析和普通聊天机器人的本质区别。

## 第二部分：从查询工具升级为经营分析工作台

[演示 BI 仪表盘]

除了自然语言查询，我还把数据分析做成了一个 BI 仪表盘。它不是静态报表，而是从数据库实时聚合业务快照，前端使用 Vue 3 和 ECharts 进行展示。

目前仪表盘包含六类指标。

第一类是客户 AUM 分布，用来识别高价值客群和服务分层。

第二类是风险收益对比，用来观察不同风险等级产品的平均收益与覆盖情况。

第三类是热销产品，可以帮助投顾和运营识别交易活跃的产品方向。

第四类是月度交易趋势，可以看到申购、赎回和转账的资金流向变化。

第五类是风险预警分布，帮助风控人员判断风险事件的数量和级别结构。

第六类是产品货架矩阵，观察 R1 到 R5 风险等级和产品类型是否覆盖均衡。

BI 仪表盘解决的是“主动看全局”的问题；自然语言查询工作台解决的是“按问题下钻”的问题。两者共享同一个数据底座，但分别服务管理驾驶舱和临时分析场景。

[切换“自然语言查询”]

现场我会输入这样一个问题：“查询资产超过一百万的客户，并按风险等级统计。”

系统会展示生成的 SELECT 语句、查询结果表格、图表、自然语言解释，以及生成、执行和解读三个阶段的耗时。这样评委不仅能看到答案，还能看到答案是如何产生的，并且可以审阅 SQL。

## 第三部分：我的亮点不是“查出来”，而是“能安全联动”

[切到“分析洞察白名单”]

如果任意一段大模型解释都可以直接影响投顾或风控，系统会非常危险。所以我没有把自由文本直接广播给其他 Agent，而是设计了分析洞察白名单。

只有查询结果中同时具有客户 ID 和数值证据时，系统才允许提取两类可验证事实。

第一类是持仓回撤超过百分之十；第二类是单周交易次数达到十次以上，并且有对应的交易金额证据。

这些事实会被包装成 analytics_insight 领域事件，先进入事务 Outbox，再由 Redis Pub/Sub 进行实时广播。下游画像和风控消费者可以把它作为推荐约束或交易频率监测依据。

这里有一个非常重要的边界：分析洞察不会直接修改客户正式的 C1 到 C5 风险等级。正式评级仍然必须经过风评问卷、规则引擎、校准和审计流程。这样做既发挥了数据分析的价值，也守住了金融合规边界。

## 第四部分：我参与的总体框架优化

[切到“总体框架优化”]

需求文档提出了五类业务 Agent、NL2SQL、Redis Pub/Sub 和三层记忆等目标。我的总体框架优化，是把这些能力从“功能清单”组织为一套可运行的公共骨架。

第一，在统一入口之后加入 Chat Orchestrator 和 Router Agent。Chat Orchestrator 统一处理输入安全、风险预检、实体追踪和 Trace；Router Agent 负责识别意图、生成路由计划并进行角色校验。这样新增一个 Agent 时，不需要重复实现这些横切能力。

第二，事件机制从单纯广播升级为事务 Outbox 加 Redis Pub/Sub。业务事实和事件意图在同一个数据库事务中写入，提交成功后才由 Relay 广播；消费者使用 event_id 和 consumer 账本保证幂等。这样可以避免数据已经变化、但消息丢失，或者事件重复造成状态不一致的问题。

第三，我参与梳理了记忆边界。Redis 保存短期会话、路由上下文和缓存；MySQL 保存交易、画像、预警、归档和审计事实；Milvus 提供语义知识召回；Neo4j 提供关系解释；MinIO 保存可追溯的原始知识文件。这样“记忆”不再是无限堆积的 Prompt，而是有权限、有时效、有事实源的上下文。

第四，系统增加了可观测性。每次请求可以记录 Trace、Agent、工具调用、时延和脱敏后的输入输出。在答辩中，这使我们能够解释一次回答到底走了什么链路；在工程上，也为排障和持续优化留下依据。

## 收尾

最后总结一下。

我负责的数据分析 Agent，把自然语言问题转成了可审阅、可执行、可解释的只读分析能力；通过 BI 仪表盘，把分散数据转成了可快速理解的经营视图；通过分析洞察白名单和事件链路，让少数有证据的结论可以安全地帮助投顾和风控。

而我参与的总体框架优化，则把各个 Agent 放进同一套权限、编排、事实、事件、记忆和审计机制中。

所以我认为，这个项目的亮点不只是“用了多个 Agent”，而是让每一个 Agent 的能力都能在金融业务边界内被复用、被追溯、被约束。我的汇报完毕，谢谢各位老师。

---

## 可能追问的简答

**问：为什么不让 LLM 直接查数据库？**

答：LLM 只生成候选 SQL，执行前必须通过只读、危险语法、敏感列、多语句和行数限制等规则。这样把“理解语言”和“执行数据操作”分离开。

**问：分析结果为什么不能直接改风险等级？**

答：分析结果可能是趋势信号，正式 C1–C5 评级属于适当性结论，必须走风评、规则、校准和审计。我们只把可验证分析事实作为投顾约束或风控监测输入。

**问：Outbox 解决了什么问题？**

答：它保证业务事实提交成功后，事件不会因为 Redis 短暂故障而丢失；消费者再通过幂等账本避免重复处理。
